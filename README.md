# FSweb

clone the repo 
`git clone https://github.com/francirigo/FSweb.git`

`cd backend`
`bundle install`
to run the server: `bundle exec rails s -p 4000`

`cd ../frontend`
`npm install`
`npm start`
